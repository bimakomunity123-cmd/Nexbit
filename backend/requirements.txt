flask==3.1.3
flask-cors==6.0.5
flask-limiter==4.1.1
gunicorn==26.2.0
sqlalchemy==2.0.52
bcrypt==5.0.0
pyjwt==2.13.0
python-dotenv==1.2.3
pydantic[email]==2.13.4
# Postgres driver — needed once DATABASE_URL points at a real Postgres
# instance (e.g. Render's managed Postgres) instead of local SQLite.
# NOTE: no prebuilt wheel for very new Python versions on Windows yet
# (e.g. 3.14) as of writing — pip tries to build from source and fails
# without pg_config installed. Harmless for local dev, which stays on
# SQLite; Render's Linux build environment has proper wheels for this.
psycopg2-binary==2.9.10
